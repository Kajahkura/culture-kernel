[package]
name = "graft-rust-canary"
version = "0.1.0"
edition = "2021"

[dependencies]
blinkhost-sdk = { path = "/opt/blinkhost-sdk/rust", version = "=1.1.0" }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
